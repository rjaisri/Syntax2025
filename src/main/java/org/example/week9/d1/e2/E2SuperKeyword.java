package org.example.week9.d1.e2;

import org.example.week8.d6.e1.Vehicle;

public class E2SuperKeyword {
    public static void main(String[] args) {

        /* Step 1: Create a `Vehicle` class with a no-argument constructor that prints a message to indicate it's called.
        // Step 2: Create a `Car` class that extends `Vehicle` and includes a no-argument constructor using `super()` to call the parent constructor.
        // Step 3: Test by creating a `Car` object, ensuring the `Vehicle` constructor is called first due to constructor chaining.
        // Step 4: Verify the output shows the message from the `Vehicle` constructor, demonstrating the inheritance hierarchy.
        */
        Car myCar = new Car();
    }

}
